import React from 'react'

const Button = ({text}) => {
    return (
        <button className='bg-[#F28D35] w-fit px-[20px] py-[12px] rounded uppercase font-semibold text-white text-[14px]'>{text}</button>
    )
}

export default Button